<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>PollBuilder.Contracts</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- The shared library carries the cross-service ProblemDetails handler and startup helpers,
         so it needs the ASP.NET Core framework reference rather than plain netstandard types. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Npgsql" Version="10.0.3" />
  </ItemGroup>

</Project>
